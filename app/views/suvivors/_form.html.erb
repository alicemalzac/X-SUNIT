<%= form_with(model: suvivor, local: true) do |form| %>
  <% if suvivor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(suvivor.errors.count, "error") %> prohibited this suvivor from being saved:</h2>

      <ul>
      <% suvivor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.number_field :age %>
  </div>

  <div class="field">
    <%= form.label :gender %>
    <%= form.text_field :gender %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
